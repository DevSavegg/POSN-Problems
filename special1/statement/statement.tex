\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{a4wide}

\begin{document}

\section*{mode}

รับค่าเข้ามาเป็นชุดข้อมูล(array) ของตัวเลขจำนวนเต็ม 10 ตัว
จากนั้นให้โปรแกรมหาค่าฐานนิยม(mode) ของชุดข้อมูลนั้น
ค่าฐานนิยมคือค่าที่เกิดขึ้นบ่อยที่สุดในชุดข้อมูล\\


\subsection*{Input}
{รับค่าเข้ามาเป็นชุดข้อมูล}{(array}{) ของตัวเลขจำนวนเต็ม 10 ตัว}\\


\subsection*{Output}
แสดงค่าฐานนิยมตัวแรกสุดที่เจอ


\end{document}
